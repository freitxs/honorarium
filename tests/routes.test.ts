// Placeholder tests (App Router is SSR; here we just keep smoke tests)
import { describe, it, expect } from "vitest";
describe("routes", () => {
  it("index smoke", () => { expect(true).toBe(true); });
});
